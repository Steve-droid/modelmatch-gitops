Driftplain umbrella chart rendered (release: {{ .Release.Name }}).

This chart is authored-and-rendered only. It is NOT installed by hand — ArgoCD owns
deployment from P14. Nothing is "Ready" from a `helm template` / `helm lint`.

Components in this render:
{{- if .Values.backend.enabled }}
  - backend  : {{ .Values.global.awsAccountId }}.dkr.ecr.{{ .Values.global.awsRegion }}.amazonaws.com/{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}  (port {{ .Values.backend.containerPort }})
{{- end }}
{{- if .Values.frontend.enabled }}
  - frontend : {{ .Values.global.awsAccountId }}.dkr.ecr.{{ .Values.global.awsRegion }}.amazonaws.com/{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}  (port {{ .Values.frontend.containerPort }})
{{- end }}

Public host rollout: configure global.appHost/apiHost, verify certificates, then set
useCustomHosts=true. Keep retainSslipHosts=true while old clients use the original URLs.
Postgres, secrets and platform controllers are separate ArgoCD applications.

Additional domain sets: {{ keys (.Values.global.additionalHosts | default dict) | sortAlpha | join ", " }}
Runtime host set: {{ .Values.global.runtimeHostSet | default "original branded/sslip hosts" }}
Select a runtimeHostSet only after DNS, trusted TLS, and OAuth origins are verified.
