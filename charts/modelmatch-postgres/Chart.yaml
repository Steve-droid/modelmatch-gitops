apiVersion: v2
name: modelmatch-postgres
description: >-
  In-cluster PostgreSQL 16 for ModelMatch — a CloudNativePG `Cluster` (1 primary
  + 1 streaming replica) on an EBS-CSI-backed PVC, plus the Alembic schema
  migration run as a PostSync Job/Helm hook (never on backend startup). P13.
type: application
# Chart version — bumped per chart change (independent of the app's SemVer).
version: 0.1.0
# The PostgreSQL major version this chart deploys (CNPG operand image tag).
appVersion: "16"
